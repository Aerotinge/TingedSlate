# Demesne type (de facto) laws (set for the PRIMARY title in a demesne but affects the whole demesne)

law_groups = {
	centralization = {
		law_type = realm
	}
	tribal_organization = {
		law_type = realm
	}
	vice_royalty = {
		law_type = realm
	}
	administration = {
		law_type = realm
	}
	feudal_contract = {
		law_type = realm
	}
	manorial_contract = {
		law_type = realm
	}
	feudal_tax = {
		law_type = realm
	}
	muslim_contract = {
		law_type = realm
	}
	muslim_tax = {
		law_type = realm
	}
	city_contract = {
		law_type = realm
	}
	city_tax = {
		law_type = realm
	}
	temple_contract = {
		law_type = realm
	}
	temple_tax = {
		law_type = realm
	}
	tribal_contract = {
		law_type = realm
	}
	tribal_tax = {
		law_type = realm
	}
}

laws = {
	# CENTRALIZATION LAWS
	##############################

	centralization_0 = {
		group = centralization

		demesne_size = 0
		vassal_limit = 10

		potential = {
			holder_scope = {
				is_nomadic = no
				is_tribal = no
				#is_vice_royalty = no
				NOR = {
					government = thema_government
					government = band_government
				}
				higher_real_tier_than = COUNT
			}
		}

		allow = {
			trigger_if = {
				limit = {
					OR = {
						has_law = centralization_1
						has_law = centralization_2
						has_law = centralization_3
						has_law = centralization_4
					}
				}
				has_law = centralization_1
			}
		}

		pass_effect = {
			# log = "centralization_0 pass ([This.GetFullBaseName])"
		}

		effect = {
			# log = "centralization_0 ([This.GetFullBaseName])"
			hidden_effect = {
				revoke_centralization_effect = yes
			}
		}

		ai_will_do = {
			factor = 0.8

			trigger = {
				trigger_if = { # Need to increase vassal limit
					limit = { has_law = centralization_1 }

					over_vassal_limit > 0
					over_max_demesne_size <= 0
				}
				trigger_else = {
					NOR = {
						has_law = centralization_0
						has_law = centralization_1
						has_law = centralization_2
						has_law = centralization_3
						has_law = centralization_4
					}
				}
			}
		}
	}

	centralization_1 = {
		group = centralization

		vassal_limit = 5
		demesne_size = 1

		potential = {
			is_law_potential = centralization_0
		}

		allow = {
			OR = {
				has_law = centralization_0
				has_law = centralization_2
			}

			trigger_if = {
				limit = {
					holder_scope = {
						is_tribal = yes
					}
				}

				OR = {
					has_law = tribal_organization_1
					has_law = tribal_organization_2
					has_law = tribal_organization_3
					has_law = tribal_organization_4
				}
			}
		}

		pass_effect = {
			# log = "centralization_1 pass ([This.GetFullBaseName])"
		}

		effect = {
			# log = "centralization_1 ([This.GetFullBaseName])"
			hidden_effect = {
				revoke_centralization_effect = yes
			}
		}

		ai_will_do = {
			factor = 0.9

			trigger = {
				trigger_if = { # Need to increase vassal limit
					limit = { has_law = centralization_2 }

					over_vassal_limit > 0
					over_max_demesne_size <= 0
				}
				trigger_else = { # Vassal limit left over
					over_vassal_limit <= 5
					over_max_demesne_size > 0
				}
			}
		}
	}

	centralization_2 = {
		group = centralization
		default = yes

		vassal_limit = 0
		demesne_size = 2

		potential = {
			is_law_potential = centralization_0
		}

		allow = {
			OR = {
				has_law = centralization_1
				has_law = centralization_3
			}

			trigger_if = {
				limit = {
					holder_scope = {
						is_tribal = yes
					}
				}

				OR = {
					has_law = tribal_organization_2
					has_law = tribal_organization_3
					has_law = tribal_organization_4
				}
			}
		}

		pass_effect = {
			# log = "centralization_2 pass ([This.GetFullBaseName])"
		}

		effect = {
			# log = "centralization_2 ([This.GetFullBaseName])"
			hidden_effect = {
				revoke_centralization_effect = yes
			}
		}

		ai_will_do = {
			factor = 1

			trigger = {
				trigger_if = { # Need to increase vassal limit
					limit = { has_law = centralization_3 }

					over_vassal_limit > 0
					over_max_demesne_size <= 0
				}
				trigger_else = { # Vassal limit left over
					over_vassal_limit <= 5
					over_max_demesne_size > 0
				}
			}
		}
	}

	centralization_3 = {
		group = centralization

		vassal_limit = -5
		demesne_size = 3

		potential = {
			is_law_potential = centralization_0
		}

		allow = {
			OR = {
				has_law = centralization_2
				has_law = centralization_4
			}

			trigger_if = {
				limit = {
					holder_scope = {
						is_tribal = yes
					}
				}
				OR = {
					has_law = tribal_organization_3
					has_law = tribal_organization_4
				}
			}
		}

		pass_effect = {
			# log = "centralization_3 pass ([This.GetFullBaseName])"
		}

		effect = {
			# log = "centralization_3 ([This.GetFullBaseName])"
			hidden_effect = {
				revoke_centralization_effect = yes
			}
		}

		ai_will_do = {
			factor = 0.9

			trigger = {
				trigger_if = { # Need to increase vassal limit
					limit = { has_law = centralization_4 }

					over_vassal_limit > 0
					over_max_demesne_size <= 0
				}
				trigger_else = { # Vassal limit left over
					over_vassal_limit <= 5
					over_max_demesne_size > 0
				}
			}
		}
	}

	centralization_4 = {
		group = centralization

		vassal_limit = -10
		demesne_size = 4

		potential = {
			is_law_potential = centralization_0
		}

		allow = {
			has_law = centralization_3

			trigger_if = {
				limit = {
					holder_scope = {
						is_tribal = yes
					}
				}

				has_law = tribal_organization_4
			}
		}

		pass_effect = {
			# log = "centralization_4 pass ([This.GetFullBaseName])"
		}

		effect = {
			# log = "centralization_4 ([This.GetFullBaseName])"
			hidden_effect = {
				revoke_centralization_effect = yes
			}
		}

		ai_will_do = {
			factor = 0.8

			trigger = {
				trigger_if = { # Vassal limit left over
					limit = { has_law = centralization_3 }

					over_vassal_limit <= 5
					over_max_demesne_size > 0
				}
			}
		}
	}

	# TRIBAL ORGANIZATION
	##############################

	tribal_organization_0 = {
		group = tribal_organization
		default = yes

		unreformed_tribal_opinion = 5

		potential = {
			holder_scope = {
				is_tribal = yes
			}
		}

		allow = {
			holder_scope = {
				show_scope_change = no

				trigger_if = {
					limit = { lower_real_tier_than = DUKE }
					higher_real_tier_than = COUNT
				}
			}

			trigger_if = {
				limit = {
					OR = {
						has_law = tribal_organization_1
						has_law = tribal_organization_2
						has_law = tribal_organization_3
						has_law = tribal_organization_4
					}
				}

				has_law = tribal_organization_1
			}
		}

		pass_effect = {
			# log = "tribal_organization_0 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					NOT = { has_title_flag = no_opinion_effect }
				}

				decreased_tribal_organization_effect = yes
			}
			else = {
				clr_title_flag = no_opinion_effect
			}
		}

		effect = {
			# log = "tribal_organization_0 ([This.GetFullBaseName])"

			if = {
				limit = { has_dlc = "Conclave" }

				hidden_effect = {
					revoke_law = law_voting_power_1
					revoke_law = war_voting_power_1
					disable_council_voting_on_issue = laws
					disable_council_voting_on_issue = enforce_peace
					disable_council_voting_on_issue = declare_war_interaction
				}
			}
			else = {
				set_allow_title_revokation = no
				#set_allow_free_infidel_revokation = no
				set_tribal_vassal_levy_control = no
				set_tribal_vassal_tax_income = no
			}

			trigger_switch = {
				on_trigger = has_law

				tribal_organization_2 = {
					custom_tooltip = { text = disables_tribal_tax_laws }
				}

				tribal_organization_3 = {
					custom_tooltip = { text = disables_tribal_levy_laws }
					custom_tooltip = { text = disallows_change_government }
				}

				tribal_organization_4 = {
					custom_tooltip = { text = disallows_change_government }
				}
			}

			hidden_effect = {
				if = {
					limit = {
						OR = {
							has_law = centralization_1
							has_law = centralization_2
							has_law = centralization_3
							has_law = centralization_4
						}
					}

					add_law = centralization_0
				}

				revoke_tribal_organization_effect = yes
				revoke_tribal_contract_effect = yes
				revoke_tribal_tax_effect = yes
			}
		}

		ai_will_do = {
			factor = 1

			trigger = {
				trigger_if = {
					limit = { has_law = tribal_organization_1 }

					# Try to increase vassal opinion, if necessary
					holder_scope = {
						any_vassal = {
							is_tribal = yes
							dislike_tribal_organization = yes

							opinion = {
								who = liege
								value < 0
							}
						}
					}
				}
			}

			mult_modifier = {
				factor = 0.5

				has_law = tribal_organization_1
				holder_scope = { dislike_tribal_organization = no }
			}
		}
	}

	tribal_organization_1 = {
		group = tribal_organization

		potential = {
			is_law_potential = tribal_organization_0
		}

		allow = {
			holder_scope = {
				show_scope_change = no

				trigger_if = {
					limit = { lower_real_tier_than = DUKE }
					higher_real_tier_than = COUNT
				}
			}

			OR = {
				has_law = tribal_organization_0
				has_law = tribal_organization_2
			}
		}

		pass_effect = {
			# log = "tribal_organization_1 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					NOT = { has_title_flag = no_opinion_effect }
				}

				if = {
					limit = { has_law = tribal_organization_0 }
					increased_tribal_organization_effect = yes
				}
				else = {
					decreased_tribal_organization_effect = yes
				}
			}
			else = {
				clr_title_flag = no_opinion_effect
			}
		}

		effect = {
			# log = "tribal_organization_1 ([This.GetFullBaseName])"

			if = {
				limit = { has_dlc = "Conclave" }

				add_law = law_voting_power_1
				add_law = war_voting_power_1

				hidden_effect = {
					revoke_law = grant_title_voting_power_1
					revoke_law = revoke_title_voting_power_1
					disable_council_voting_on_issue = grant_landed_title_interaction
					disable_council_voting_on_issue = revoke_title_interaction
				}
			}
			else = {
				set_allow_title_revokation = yes
				#set_allow_free_infidel_revokation = no
				set_tribal_vassal_levy_control = no
				set_tribal_vassal_tax_income = no
			}

			trigger_switch = {
				on_trigger = has_law

				tribal_organization_2 = {
					custom_tooltip = { text = disables_tribal_tax_laws }
				}

				tribal_organization_3 = {
					custom_tooltip = { text = disables_tribal_levy_laws }
					custom_tooltip = { text = disallows_change_government }
				}

				tribal_organization_4 = {
					custom_tooltip = { text = disallows_change_government }
				}
			}

			hidden_effect = {
				revoke_tribal_organization_effect = yes
				revoke_tribal_contract_effect = yes
				revoke_tribal_tax_effect = yes
			}
		}

		ai_will_do = {
			factor = 1

			trigger = {
				trigger_if = {
					limit = { has_law = tribal_organization_2 }

					# Try to increase vassal opinion, if necessary
					holder_scope = {
						any_vassal = {
							is_tribal = yes
							dislike_tribal_organization = yes

							opinion = {
								who = liege
								value < 0
							}
						}
					}
				}
			}

			mult_modifier = {
				factor = 0.5

				has_law = tribal_organization_0

				holder_scope = {
					any_vassal = {
						is_tribal = yes
						dislike_tribal_organization = yes

						opinion = {
							who = liege
							value < 10
						}
					}
				}
			}

			mult_modifier = {
				factor = 2

				has_law = tribal_organization_0
				holder_scope = { dislike_tribal_organization = no }
			}

			mult_modifier = {
				factor = 0.5

				has_law = tribal_organization_2
				holder_scope = { dislike_tribal_organization = no }
			}
		}
	}

	tribal_organization_2 = {
		group = tribal_organization

		unreformed_tribal_opinion = -5

		potential = {
			is_law_potential = tribal_organization_0
		}

		allow = {
			holder_scope = {
				show_scope_change = no

				trigger_if = {
					limit = { lower_real_tier_than = DUKE }
					higher_real_tier_than = COUNT
				}
			}

			OR = {
				has_law = tribal_organization_1
				has_law = tribal_organization_3
			}
			trigger_if = {
				limit = {
					holder_scope = {
						government = nomadic_tribal_government
					}
				}

				holder_scope = {
					NOT = { government = nomadic_tribal_government }
				}
			}
		}

		pass_effect = {
			# log = "tribal_organization_2 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					NOT = { has_title_flag = no_opinion_effect }
				}

				if = {
					limit = { has_law = tribal_organization_1 }
					increased_tribal_organization_effect = yes
				}
				else = {
					decreased_tribal_organization_effect = yes
				}
			}
			else = {
				clr_title_flag = no_opinion_effect
			}
		}

		effect = {
			# log = "tribal_organization_2 ([This.GetFullBaseName])"

			if = {
				limit = { has_dlc = "Conclave" }

				add_law = grant_title_voting_power_1
				add_law = revoke_title_voting_power_1

				hidden_effect = {
					revoke_law = imprison_voting_power_1
					revoke_law = banish_voting_power_1
					revoke_law = execution_voting_power_1
					disable_council_voting_on_issue = imprison_character_interaction
					disable_council_voting_on_issue = exile_imprisoned_interaction
					disable_council_voting_on_issue = execute_imprisoned_interaction
				}
			}
			else = {
				set_allow_title_revokation = yes
				#set_allow_free_infidel_revokation = yes
				set_tribal_vassal_levy_control = no
				set_tribal_vassal_tax_income = yes
			}

			if = {
				limit = {
					OR = {
						has_law = tribal_organization_0
						has_law = tribal_organization_1
					}
				}

				if = {
					limit = { NOT = { has_dlc = "Conclave" } }

					custom_tooltip = {
						text = enables_tribal_tax_laws
						add_law = tribal_tax_0
					}
				}
			}
			else_if = {
				limit = {
					OR = {
						has_law = tribal_organization_3
						has_law = tribal_organization_4
					}
				}

				custom_tooltip = { text = disallows_change_government }
			}

			hidden_effect = {
				revoke_tribal_organization_effect = yes
				revoke_tribal_contract_effect = yes
				revoke_tribal_tax_effect = yes
			}
		}

		ai_will_do = {
			factor = 1

			trigger = {
				trigger_if = {
					limit = { has_law = tribal_organization_3 }

					# Try to increase vassal opinion, if necessary
					holder_scope = {
						any_vassal = {
							is_tribal = yes
							dislike_tribal_organization = yes

							opinion = {
								who = liege
								value < 0
							}
						}
					}
				}
			}

			mult_modifier = {
				factor = 0.5

				has_law = tribal_organization_1

				holder_scope = {
					any_vassal = {
						is_tribal = yes
						dislike_tribal_organization = yes

						opinion = {
							who = liege
							value < 5
						}
					}
				}
			}

			mult_modifier = {
				factor = 2

				has_law = tribal_organization_1
				holder_scope = { dislike_tribal_organization = no }
			}

			mult_modifier = {
				factor = 0.5

				has_law = tribal_organization_3
				holder_scope = { dislike_tribal_organization = no }
			}
		}
	}

	tribal_organization_3 = {
		group = tribal_organization

		unreformed_tribal_opinion = -10
		demesne_size = -1
		vassal_limit = 5

		potential = {
			is_law_potential = tribal_organization_0
		}

		allow = {
			holder_scope = {
				show_scope_change = no

				trigger_if = {
					limit = { lower_real_tier_than = DUKE }
					higher_real_tier_than = COUNT
				}
			}

			OR = {
				has_law = tribal_organization_2
				has_law = tribal_organization_4
			}
			trigger_if = {
				limit = {
					holder_scope = {
						government = nomadic_tribal_government
					}
				}

				holder_scope = {
					NOT = { government = nomadic_tribal_government }
				}
			}
		}

		pass_effect = {
			# log = "tribal_organization_3 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					NOT = { has_title_flag = no_opinion_effect }
				}

				if = {
					limit = { has_law = tribal_organization_2 }
					increased_tribal_organization_effect = yes
				}
				else = {
					decreased_tribal_organization_effect = yes
				}
			}
			else = {
				clr_title_flag = no_opinion_effect
			}
		}

		effect = {
			# log = "tribal_organization_3 ([This.GetFullBaseName])"

			if = {
				limit = { has_dlc = "Conclave" }

				add_law = imprison_voting_power_1
				add_law = banish_voting_power_1
				add_law = execution_voting_power_1

				hidden_effect = {
					add_law = law_voting_power_1
					add_law = war_voting_power_1
					add_law = grant_title_voting_power_1
					add_law = revoke_title_voting_power_1
				}
			}
			else = {
				set_allow_title_revokation = yes
				#set_allow_free_infidel_revokation = yes
				set_tribal_vassal_levy_control = yes
				set_tribal_vassal_tax_income = yes
			}

			if = {
				limit = {
					OR = {
						has_law = tribal_organization_0
						has_law = tribal_organization_1
						has_law = tribal_organization_2
					}
				}

				if = {
					limit = { NOT = { has_dlc = "Conclave" } }

					custom_tooltip = {
						text = enables_tribal_levy_laws
						add_law = tribal_contract_0
					}
				}

				if = {
					limit = {
						holder_scope = { is_not_unreformed_pagan_trigger = yes }
					}

					custom_tooltip = { text = allows_change_government }
				}
			}
			else_if = {
				limit = { has_law = tribal_organization_4 }

				if = {
					limit = {
						holder_scope = { is_unreformed_pagan_trigger = yes }
					}

					custom_tooltip = { text = disallows_change_government }
				}
			}

			hidden_effect = {
				revoke_tribal_organization_effect = yes
			}
		}

		ai_will_do = {
			factor = 1

			trigger = {
				trigger_if = {
					limit = { has_law = tribal_organization_4 }

					# Try to increase vassal opinion, if necessary
					holder_scope = {
						any_vassal = {
							is_tribal = yes
							dislike_tribal_organization = yes

							opinion = {
								who = liege
								value < 0
							}
						}
					}
				}
			}

			mult_modifier = {
				factor = 0.5

				has_law = tribal_organization_2

				holder_scope = {
					any_vassal = {
						is_tribal = yes
						dislike_tribal_organization = yes

						opinion = {
							who = liege
							value < 10
						}
					}
				}
			}

			mult_modifier = {
				factor = 2

				has_law = tribal_organization_2
				holder_scope = { dislike_tribal_organization = no }
			}

			mult_modifier = {
				factor = 0.5

				has_law = tribal_organization_4
				holder_scope = { dislike_tribal_organization = no }
			}
		}
	}

	tribal_organization_4 = {
		group = tribal_organization

		unreformed_tribal_opinion = -15
		demesne_size = -2
		vassal_limit = 10

		potential = {
			is_law_potential = tribal_organization_0
		}

		allow = {
			holder_scope = {
				show_scope_change = no

				trigger_if = {
					limit = { lower_real_tier_than = DUKE }
					higher_real_tier_than = COUNT
				}
			}

			has_law = tribal_organization_3
			trigger_if = {
				limit = {
					holder_scope = {
						government = nomadic_tribal_government
					}
				}

				holder_scope = {
					NOT = { government = nomadic_tribal_government }
				}
			}
		}

		pass_effect = {
			# log = "tribal_organization_4 pass ([This.GetFullBaseName])"

			if = {
				limit = {
					NOT = { has_title_flag = no_opinion_effect }
				}

				increased_tribal_organization_effect = yes
			}
			else = {
				clr_title_flag = no_opinion_effect
			}
		}

		effect = {
			# log = "tribal_organization_4 ([This.GetFullBaseName])"

			if = {
				limit = { NOT = { has_dlc = "Conclave" } }

				set_allow_title_revokation = yes
				#set_allow_free_infidel_revokation = yes
				set_tribal_vassal_levy_control = yes
				set_tribal_vassal_tax_income = yes

				if = {
					limit = {
						OR = {
							has_law = tribal_organization_0
							has_law = tribal_organization_1
							has_law = tribal_organization_2
						}
					}

					custom_tooltip = {
						text = enables_tribal_levy_laws
						add_law = tribal_contract_0
					}
				}
			}

			if = {
				limit = {
					OR = {
						has_law = tribal_organization_0
						has_law = tribal_organization_1
						has_law = tribal_organization_2
						has_law = tribal_organization_3
					}
				}

				custom_tooltip = { text = allows_change_government }
			}

			hidden_effect = {
				revoke_tribal_organization_effect = yes
			}
		}

		ai_will_do = {
			factor = 1

			mult_modifier = {
				factor = 0.5

				has_law = tribal_organization_3

				holder_scope = {
					any_vassal = {
						is_tribal = yes
						dislike_tribal_organization = yes

						opinion = {
							who = liege
							value < 10
						}
					}
				}
			}

			mult_modifier = {
				factor = 2

				has_law = tribal_organization_3
				holder_scope = { dislike_tribal_organization = no }
			}
		}
	}

	# VICE ROYALTY LAWS
	##############################

	vice_royalty_0 = {
		group = vice_royalty
		default = yes

		potential = {
			has_dlc = "Charlemagne"
			higher_real_tier_than = DUKE

			holder_scope = {
				is_feudal = yes
				NOT = { government = arabic_caliphal_government }
			}
		}

		allow = {
			holder_scope = {
				NOT = {	government = roman_imperial_government }
			}
		}

		pass_effect = {
			# log = "vice_royalty_0 pass ([This.GetFullBaseName])"
		}

		effect = {
			# log = "vice_royalty_0 ([This.GetFullBaseName])"

			set_allow_vice_royalties = no

			hidden_effect = {
				revoke_vice_royalty_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	vice_royalty_1 = {
		group = vice_royalty

		vassal_limit = -10

		potential = {
			is_law_potential = vice_royalty_0
		}

		allow = {
			trigger_if = {
				limit = {
					NOT = { has_law = crown_authority_4 }
				}
				has_law = crown_authority_3
				has_law = centralization_4
			}
			trigger_else = {
				has_law = crown_authority_4
				OR = {
					has_law = centralization_3
					has_law = centralization_4
				}
			}
		}

		pass_effect = {
			# log = "vice_royalty_1 pass ([This.GetFullBaseName])"
		}

		effect = {
			# log = "vice_royalty_1 ([This.GetFullBaseName])"

			set_allow_vice_royalties = DUKE

			hidden_effect = {
				revoke_vice_royalty_effect = yes
			}
		}

		ai_will_do = {
			factor = 3

			trigger = {
				# Must have enough vassal limit left over
				has_law = vice_royalty_0
				over_vassal_limit <= -10
			}
		}
	}

	# BYZANTINE ADMINISTRATION
	##############################

	feudal_administration = {
		group = administration

		potential = {
			NOT = { has_dlc = "Conclave" }
			real_tier = EMPEROR

			holder_scope = {
				OR = {
					government = feudal_government
					government = roman_imperial_government
				}
			}
		}

		allow = {
			holder_scope = {
				NOT = {
					government = roman_imperial_government
				}
			}
		}

		pass_effect = {
			# log = "feudal_administration pass ([This.GetFullBaseName])"
		}

		effect = {
			# log = "feudal_administration ([This.GetFullBaseName])"

			if = {
				limit = { has_dlc = "Charlemagne" }

				if = {
					limit = {
						NOT = { is_law_allowed = vice_royalty_1 }
					}
					add_law = vice_royalty_0
				}
				set_allow_free_vice_royalty_revokation = no
			}
			else = {
				set_allow_free_duchy_revokation = no
			}

			revoke_law = imperial_administration
		}

		ai_will_do = {
			factor = 0
		}
	}

	imperial_administration = {
		group = administration

		vassal_limit = 25
		feudal_opinion = -10

		potential = {
			is_law_potential = feudal_administration
		}

		allow = {
			has_law = crown_authority_4
		}

		pass_effect = {
			# log = "imperial_administration pass ([This.GetFullBaseName])"
		}

		effect = {
			# log = "imperial_administration ([This.GetFullBaseName])"

			if = {
				limit = { has_dlc = "Charlemagne" }
				add_law = vice_royalty_1
				set_allow_free_vice_royalty_revokation = yes
			}
			else = {
				set_allow_free_duchy_revokation = yes
			}

			revoke_law = feudal_administration
		}

		ai_will_do = {
			factor = 10
		}
	}

	# FEUDAL VASSAL LEVY SIZE LAWS
	##############################

	feudal_contract_0 = {
		group = feudal_contract

		feudal_opinion = 10
		castle_vassal_max_levy = -0.40
		castle_vassal_min_levy = 0.10

		potential = {
			NOT = { has_dlc = "Conclave" }

			holder_scope = {
				is_playable = yes
				OR = {
					government = feudal_government
					government = tibetan_feudal_government
					government = thema_government
				}
			}

			NOT = { is_law_potential = muslim_contract_0 }
		}

		effect = {
			hidden_effect = {
				revoke_feudal_contract_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	feudal_contract_1 = {
		group = feudal_contract

		feudal_opinion = 5
		castle_vassal_max_levy = -0.30
		castle_vassal_min_levy = 0.10

		potential = {
			is_law_potential = feudal_contract_0
		}

		effect = {
			hidden_effect = {
				revoke_feudal_contract_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	feudal_contract_2 = {
		group = feudal_contract
		default = yes

		castle_vassal_max_levy = -0.20
		castle_vassal_min_levy = 0.10

		potential = {
			is_law_potential = feudal_contract_0
		}

		effect = {
			hidden_effect = {
				revoke_feudal_contract_effect = yes
				revoke_manorial_contract_effect = yes
				revoke_muslim_contract_effect = yes
			}
		}

		ai_will_do = {
			factor = 1
		}
	}

	feudal_contract_3 = {
		group = feudal_contract

		feudal_opinion = -5
		castle_vassal_min_levy = 0.10

		potential = {
			is_law_potential = feudal_contract_0
		}

		effect = {
			hidden_effect = {
				revoke_feudal_contract_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	# MANORIAL VASSAL LEVY SIZE LAWS
	##############################

	manorial_contract_0 = {
		group = manorial_contract

		feudal_opinion = 10
		castle_vassal_max_levy = -0.40

		potential = {
			NOT = { has_dlc = "Conclave" }

			holder_scope = {
				is_playable = yes
			}

			NOR = {
				is_law_potential = feudal_contract_0
				is_law_potential = muslim_contract_0
			}
		}

		effect = {
			hidden_effect = {
				revoke_manorial_contract_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	manorial_contract_1 = {
		group = manorial_contract

		feudal_opinion = 5
		castle_vassal_max_levy = -0.30

		potential = {
			is_law_potential = manorial_contract_0
		}

		effect = {
			hidden_effect = {
				revoke_manorial_contract_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	manorial_contract_2 = {
		group = manorial_contract
		default = yes

		castle_vassal_max_levy = -0.20

		potential = {
			is_law_potential = manorial_contract_0
		}

		effect = {
			hidden_effect = {
				revoke_feudal_contract_effect = yes
				revoke_manorial_contract_effect = yes
				revoke_muslim_contract_effect = yes
			}
		}

		ai_will_do = {
			factor = 1
		}
	}

	manorial_contract_3 = {
		group = manorial_contract

		feudal_opinion = -5

		potential = {
			is_law_potential = manorial_contract_0
		}

		effect = {
			hidden_effect = {
				revoke_manorial_contract_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	# FEUDAL VASSAL TAX SIZE LAWS
	##############################

	feudal_tax_0 = {
		group = feudal_tax
		default = yes

		potential = {
			NOT = { has_dlc = "Conclave" }

			holder_scope = {
				is_playable = yes

				trigger_if = {
					limit = { is_tribal = yes }

					OR = {
						has_law = tribal_organization_2
						has_law = tribal_organization_3
						has_law = tribal_organization_4
					}
				}
			}

			NOT = { is_law_potential = muslim_tax_0 }
		}

		effect = {
			hidden_effect = {
				revoke_feudal_tax_effect = yes
				revoke_muslim_tax_effect = yes
			}
		}

		ai_will_do = {
			factor = 1
		}
	}

	feudal_tax_1 = {
		group = feudal_tax
		feudal_opinion = -10
		castle_vassal_tax_modifier = 0.10

		potential = {
			is_law_potential = feudal_tax_0
		}

		effect = {
			hidden_effect = {
				revoke_feudal_tax_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	feudal_tax_2 = {
		group = feudal_tax

		feudal_opinion = -20
		castle_vassal_tax_modifier = 0.20

		potential = {
			is_law_potential = feudal_tax_0
		}

		effect = {
			hidden_effect = {
				revoke_feudal_tax_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	feudal_tax_3 = {
		group = feudal_tax

		feudal_opinion = -30
		castle_vassal_tax_modifier = 0.30

		potential = {
			is_law_potential = feudal_tax_0
		}

		effect = {
			hidden_effect = {
				revoke_feudal_tax_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	# MUSLIM VASSAL LEVY SIZE LAWS
	##############################

	muslim_contract_0 = {
		group = muslim_contract

		feudal_opinion = 5
		temple_opinion = 5
		castle_vassal_max_levy = -0.40
		temple_vassal_max_levy = -0.50

		potential = {
			NOT = { has_dlc = "Conclave" }

			holder_scope = {
				is_playable = yes
				religion_group = muslim
			}
		}

		effect = {
			hidden_effect = {
				revoke_muslim_contract_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	muslim_contract_1 = {
		group = muslim_contract
		default = yes

		castle_vassal_max_levy = -0.30
		temple_vassal_max_levy = -0.40

		potential = {
			is_law_potential = muslim_contract_0
		}

		effect = {
			hidden_effect = {
				revoke_feudal_contract_effect = yes
				revoke_manorial_contract_effect = yes
				revoke_muslim_contract_effect = yes
			}
		}

		ai_will_do = {
			factor = 1
		}
	}

	muslim_contract_2 = {
		group = muslim_contract

		feudal_opinion = -5
		temple_opinion = -5
		castle_vassal_max_levy = -0.20
		temple_vassal_max_levy = -0.30

		potential = {
			is_law_potential = muslim_contract_0
		}

		effect = {
			hidden_effect = {
				revoke_muslim_contract_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	muslim_contract_3 = {
		group = muslim_contract

		feudal_opinion = -10
		temple_opinion = -10
		temple_vassal_max_levy = -0.20

		potential = {
			is_law_potential = muslim_contract_0
		}

		effect = {
			hidden_effect = {
				revoke_muslim_contract_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	# MUSLIM VASSAL TAX SIZE LAWS
	##############################

	muslim_tax_0 = {
		group = muslim_tax
		default = yes

		castle_vassal_tax_modifier = 0.05
		temple_vassal_tax_modifier = 0.25

		potential = {
			NOT = { has_dlc = "Conclave" }

			holder_scope = {
				is_playable = yes
				religion_group = muslim

				trigger_if = {
					limit = { is_tribal = yes }

					OR = {
						has_law = tribal_organization_2
						has_law = tribal_organization_3
						has_law = tribal_organization_4
					}
				}
			}
		}

		effect = {
			hidden_effect = {
				revoke_feudal_tax_effect = yes
				revoke_muslim_tax_effect = yes
			}
		}

		ai_will_do = {
			factor = 1
		}
	}

	muslim_tax_1 = {
		group = muslim_tax

		feudal_opinion = -10
		temple_opinion = -10
		castle_vassal_tax_modifier = 0.15
		temple_vassal_tax_modifier = 0.35

		potential = {
			is_law_potential = muslim_tax_0
		}

		effect = {
			hidden_effect = {
				revoke_muslim_tax_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	muslim_tax_2 = {
		group = muslim_tax

		feudal_opinion = -20
		temple_opinion = -20
		castle_vassal_tax_modifier = 0.25
		temple_vassal_tax_modifier = 0.45

		potential = {
			is_law_potential = muslim_tax_0
		}

		effect = {
			hidden_effect = {
				revoke_muslim_tax_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	muslim_tax_3 = {
		group = muslim_tax

		feudal_opinion = -30
		temple_opinion = -30
		castle_vassal_tax_modifier = 0.35
		temple_vassal_tax_modifier = 0.55

		potential = {
			is_law_potential = muslim_tax_0
		}

		effect = {
			hidden_effect = {
				revoke_muslim_tax_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	# CITY VASSAL LEVY SIZE LAWS
	##############################

	city_contract_0 = {
		group = city_contract

		city_opinion = 10
		city_vassal_max_levy = -0.50

		potential = {
			NOT = { has_dlc = "Conclave" }

			holder_scope = {
				is_playable = yes
			}
		}

		pass_effect = {
			# log = "city_contract_0 pass ([This.GetFullBaseName])"
		}

		effect = {
			hidden_effect = {
				revoke_city_contract_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	city_contract_1 = {
		group = city_contract
		default = yes

		city_vassal_max_levy = -0.35

		potential = {
			is_law_potential = city_contract_0
		}

		effect = {
			hidden_effect = {
				revoke_city_contract_effect = yes
			}
		}

		ai_will_do = {
			factor = 1
		}
	}

	city_contract_2 = {
		group = city_contract

		city_opinion = -10
		city_vassal_max_levy = -0.20

		potential = {
			is_law_potential = city_contract_0
		}

		effect = {
			hidden_effect = {
				revoke_city_contract_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	city_contract_3 = {
		group = city_contract

		city_opinion = -20
		city_vassal_max_levy = -0.05

		potential = {
			is_law_potential = city_contract_0
		}

		effect = {
			hidden_effect = {
				revoke_city_contract_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	# CITY VASSAL TAX SIZE LAWS
	##############################

	city_tax_0 = {
		group = city_tax

		city_opinion = 10
		city_vassal_tax_modifier = 0.15

		potential = {
			NOT = { has_dlc = "Conclave" }

			holder_scope = {
				is_playable = yes

				trigger_if = {
					limit = { is_tribal = yes }

					OR = {
						has_law = tribal_organization_2
						has_law = tribal_organization_3
						has_law = tribal_organization_4
					}
				}
			}
		}

		effect = {
			hidden_effect = {
				revoke_city_tax_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	city_tax_1 = {
		group = city_tax
		default = yes

		city_vassal_tax_modifier = 0.25

		potential = {
			is_law_potential = city_tax_0
		}

		effect = {
			hidden_effect = {
				revoke_city_tax_effect = yes
			}
		}

		ai_will_do = {
			factor = 1
		}
	}

	city_tax_2 = {
		group = city_tax

		city_opinion = -10
		city_vassal_tax_modifier = 0.35

		potential = {
			is_law_potential = city_tax_0
		}

		effect = {
			hidden_effect = {
				revoke_city_tax_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	city_tax_3 = {
		group = city_tax

		city_opinion = -20
		city_vassal_tax_modifier = 0.45

		potential = {
			is_law_potential = city_tax_0
		}

		effect = {
			hidden_effect = {
				revoke_city_tax_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	# TEMPLE VASSAL LEVY SIZE LAWS
	##############################

	temple_contract_0 = {
		group = temple_contract

		temple_opinion = 10
		temple_vassal_max_levy = -0.50

		potential = {
			NOT = { has_dlc = "Conclave" }

			holder_scope = {
				is_playable = yes
			}

			NOT = { is_law_potential = muslim_contract_0 }
		}

		effect = {
			hidden_effect = {
				revoke_temple_contract_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	temple_contract_1 = {
		group = temple_contract
		default = yes

		temple_vassal_max_levy = -0.40

		potential = {
			is_law_potential = temple_contract_0
		}

		effect = {
			hidden_effect = {
				revoke_temple_contract_effect = yes
				revoke_muslim_contract_effect = yes
			}
		}

		ai_will_do = {
			factor = 1
		}
	}

	temple_contract_2 = {
		group = temple_contract

		temple_opinion = -10
		temple_vassal_max_levy = -0.30

		potential = {
			is_law_potential = temple_contract_0
		}

		effect = {
			hidden_effect = {
				revoke_temple_contract_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	temple_contract_3 = {
		group = temple_contract

		temple_opinion = -20
		temple_vassal_max_levy = -0.20

		potential = {
			is_law_potential = temple_contract_0
		}

		effect = {
			hidden_effect = {
				revoke_temple_contract_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	# TEMPLE VASSAL TAX SIZE LAWS
	##############################

	temple_tax_0 = {
		group = temple_tax

		temple_opinion = 20

		potential = {
			NOT = { has_dlc = "Conclave" }

			holder_scope = {
				is_playable = yes

				trigger_if = {
					limit = { is_tribal = yes }

					OR = {
						has_law = tribal_organization_2
						has_law = tribal_organization_3
						has_law = tribal_organization_4
					}
				}
			}

			NOT = { is_law_potential = muslim_tax_0 }
		}

		effect = {
			hidden_effect = {
				revoke_temple_tax_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	temple_tax_1 = {
		group = temple_tax
		default = yes

		temple_opinion = 0
		temple_vassal_tax_modifier = 0.35

		potential = {
			is_law_potential = temple_tax_0
		}

		effect = {
			hidden_effect = {
				revoke_temple_tax_effect = yes
				revoke_muslim_tax_effect = yes
			}
		}

		ai_will_do = {
			factor = 1
		}
	}

	temple_tax_2 = {
		group = temple_tax

		temple_opinion = -10
		temple_vassal_tax_modifier = 0.45

		potential = {
			is_law_potential = temple_tax_0
		}

		effect = {
			hidden_effect = {
				revoke_temple_tax_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	temple_tax_3 = {
		group = temple_tax

		temple_opinion = -20
		temple_vassal_tax_modifier = 0.55

		potential = {
			is_law_potential = temple_tax_0
		}

		effect = {
			hidden_effect = {
				revoke_temple_tax_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	# TRIBAL VASSAL LEVY SIZE LAWS
	##############################

	tribal_contract_0 = {
		group = tribal_contract

		tribal_vassal_max_levy = -0.50

		potential = {
			NOT = { has_dlc = "Conclave" }

			holder_scope = {
				is_playable = yes

				trigger_if = {
					limit = { is_tribal = yes }

					OR = {
						has_law = tribal_organization_3
						has_law = tribal_organization_4
					}
				}
			}
		}

		effect = {
			hidden_effect = {
				revoke_tribal_contract_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	tribal_contract_1 = {
		group = tribal_contract
		default = yes

		tribal_opinion = -10
		tribal_vassal_max_levy = -0.40

		potential = {
			is_law_potential = tribal_contract_0
		}

		effect = {
			hidden_effect = {
				revoke_tribal_contract_effect = yes
			}
		}

		ai_will_do = {
			factor = 1
		}
	}

	tribal_contract_2 = {
		group = tribal_contract

		tribal_opinion = -20
		tribal_vassal_max_levy = -0.30

		potential = {
			is_law_potential = tribal_contract_0
		}

		effect = {
			hidden_effect = {
				revoke_tribal_contract_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	tribal_contract_3 = {
		group = tribal_contract

		tribal_opinion = -30
		tribal_vassal_max_levy = -0.20

		potential = {
			is_law_potential = tribal_contract_0
		}

		effect = {
			hidden_effect = {
				revoke_tribal_contract_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	# TRIBAL VASSAL TAX SIZE LAWS
	##############################

	tribal_tax_0 = {
		group = tribal_tax
		default = yes

		potential = {
			NOT = { has_dlc = "Conclave" }

			holder_scope = {
				is_playable = yes

				trigger_if = {
					limit = { is_tribal = yes }

					OR = {
						has_law = tribal_organization_2
						has_law = tribal_organization_3
						has_law = tribal_organization_4
					}
				}
			}
		}

		effect = {
			hidden_effect = {
				revoke_tribal_tax_effect = yes
			}
		}

		ai_will_do = {
			factor = 1
		}
	}

	tribal_tax_1 = {
		group = tribal_tax

		tribal_opinion = -10
		tribal_vassal_tax_modifier = 0.10

		potential = {
			is_law_potential = tribal_tax_0
		}

		effect = {
			hidden_effect = {
				revoke_tribal_tax_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	tribal_tax_2 = {
		group = tribal_tax

		tribal_opinion = -20
		tribal_vassal_tax_modifier = 0.20

		potential = {
			is_law_potential = tribal_tax_0
		}

		effect = {
			hidden_effect = {
				revoke_tribal_tax_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}

	tribal_tax_3 = {
		group = tribal_tax

		tribal_opinion = -30
		tribal_vassal_tax_modifier = 0.30

		potential = {
			is_law_potential = tribal_tax_0
		}

		effect = {
			hidden_effect = {
				revoke_tribal_tax_effect = yes
			}
		}

		ai_will_do = {
			factor = 0
		}
	}
}